echo "temporary code for search" > exp3.txt
git add .
git status
git stash -m "added file for search"

git branch feature
git branch

echo "this is file 2" > exp3.1.txt
git add .
git stash -m "stashing file 2"

echo "this is file 3" > exp3.2.txt
git add .
git stash -m "stashing file 3"

echo "this is file 4" > exp3.3.txt
git add .
git stash -m "stashing file 4"

git stash list
git checkout feature
ls
git stash apply

git stash apply 1
git commit -m "saving all the stash"

git checkout -
git stash list
git stash drop
git stash list
git stash clear
git stash list
