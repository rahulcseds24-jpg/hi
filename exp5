echo "# expriment-5" >> README.md
ls
git status
git add README.md
git status
git commit -m "First commit to remote repository"

git branch
git branch -M main
git branch

git remote add origin git@github.com:Srujan-h/expriment-5.git
ssh-keygen -t rsa -b 4096 -C "srujan.csedcs24@cmrit.ac.in"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub
ssh -T git@github.com

git push -v origin main

  /* do till here for tags experiment and for 5th experiment*/
  
  
git branch
git branch fb
git branch
git checkout fb
git add README.md
git add .
git commit -m "Add changes to fb branch"
git checkout main
git merge fb

git fetch origin
git rebase origin/main
git push origin main
